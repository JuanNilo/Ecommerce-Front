/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.discordapp.com','https://upload.wikimedia.org/'],
    }
}

module.exports = nextConfig

// experimental: { appDir: true}
